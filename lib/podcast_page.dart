import 'package:flutter/material.dart';

class PodcastPageView extends StatelessWidget {
  const PodcastPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}